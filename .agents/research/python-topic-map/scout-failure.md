---
title: Python failure corpus — landscape scout
corpus: failure corpus (footguns, concurrency, subprocess/CLI, testing, packaging, security, reviewer objections, postmortems)
agent: scout-failure
model: sonnet
date_researched: 2026-08-22
sources_count: 34
---

## Table of contents

- [Failure catalogue](#failure-catalogue)
- [Recurring reviewer objections](#recurring-reviewer-objections)
- [Candidate topics](#candidate-topics)
- [Sources](#sources)

## Failure catalogue

| Failure | Mechanism (one line) | Still true in 3.14? | Caught by which lint/check | Source |
|---|---|---|---|---|
| Mutable default argument | Default evaluated once at `def` time; shared across calls | Yes | `ruff` B006 (misses named-constant aliases — false negative, [ruff#21142](https://github.com/astral-sh/ruff/issues/21142)) | [ruff B006 docs](https://docs.astral.sh/ruff/rules/mutable-argument-default/) |
| Late-binding closures in loops/comprehensions | Closures capture the variable, not its value at creation time | Yes, unchanged language semantics | `ruff` B023 (`function-uses-loop-variable`) | [ruff B023](https://docs.astral.sh/ruff/rules/function-uses-loop-variable/) |
| `is` used with int/string literals | Small-int (-5..256) and some string interning is a CPython implementation detail, not guaranteed | Yes | `ruff`/pycodestyle F632 (`is-literal`), SyntaxWarning since 3.8 | [CPython SyntaxWarning "is" with literal](https://github.com/python/cpython/issues/79031) |
| Shallow vs deep copy confusion | `copy.copy`/`list(x)`/slicing copy one level; nested mutables still shared | Yes | nothing (needs code review / `copy.deepcopy` discipline) | [copy docs](https://docs.python.org/3/library/copy.html) |
| `list * n` aliasing | `[[]] * n` creates n references to the same inner list | Yes | nothing automated; `ruff` has no direct rule | [Python FAQ](https://docs.python.org/3/faq/programming.html) |
| Truthiness of empty containers vs `None` | `if x:` is False for `[]`, `{}`, `0`, `""` and `None` alike — masks "not set" vs "empty" | Yes | `ruff` PLC1901 (`compare-to-empty-string`) partial coverage only | [PEP 8](https://peps.python.org/pep-0008/) |
| `dict`/`set` mutation during iteration | Raises `RuntimeError: dictionary changed size during iteration`; size-preserving mutation can silently skip/repeat keys | Yes, enforcement got stricter in 3.14 (also fires from GC on WeakKeyDictionary) | nothing (no static lint catches all cases) | [bpo-35615](https://bugs.python.org/issue35615) |
| `str`/`bytes` confusion, implicit encoding | Mixing `str` and `bytes` raises `TypeError`; implicit encode/decode uses locale default, not always UTF-8 | Yes; `PYTHONUTF8`/UTF-8 mode reduces but doesn't eliminate it | `mypy`/`pyright` (type checker flags str/bytes mismatch) | [Python I/O docs](https://docs.python.org/3/tutorial/inputoutput.html) |
| Float formatting / `Decimal` needed for money | Binary floats can't represent most decimals exactly; `0.1+0.2 != 0.3` | Yes | nothing generic; `flake8-decimal` style project-specific lints only | [decimal docs](https://docs.python.org/3/library/decimal.html) |
| `datetime.utcnow()` naivety | Returns a naive datetime that silently means UTC but carries no tzinfo, easy to mix with aware datetimes | **Deprecated in 3.12, scheduled for removal** — no longer just a "gotcha," it's a `DeprecationWarning` | `ruff` DTZ003 (flake8-datetimez) | [utcnow deprecation](https://blog.miguelgrinberg.com/post/it-s-time-for-a-change-datetime-utcnow-is-now-deprecated) |
| DST / naive datetime arithmetic | Adding `timedelta` to a naive local datetime silently crosses DST boundaries wrong | Yes | nothing generic; use `zoneinfo` + aware datetimes | [zoneinfo docs](https://docs.python.org/3/library/zoneinfo.html) |
| `__del__` and object resurrection | `tp_finalize` can re-add a reference and resurrect an object mid-GC | Yes, still possible at the C-API level | nothing | [gc docs](https://docs.python.org/3/library/gc.html) |
| Circular imports | Module partially initialized in `sys.modules` before its body finishes running | Yes | nothing static; surfaces as `ImportError`/`AttributeError` at runtime | [Ned Batchelder: fixing circular imports](https://nedbatchelder.com/blog/202405/one_way_to_fix_python_circular_imports.html) |
| `__init__.py` / namespace-package shadowing | A directory without `__init__.py` becomes an implicit namespace package that can silently shadow an intended regular package on `sys.path` | Yes | nothing automated | [PEP 420](https://peps.python.org/pep-0420/) |
| Module shadowing a stdlib name | A local file named e.g. `random.py` or `queue.py` shadows the stdlib module for every import in that directory | Yes | `ruff` A005 (`stdlib-module-shadowing`) | [ruff A005](https://docs.astral.sh/ruff/rules/builtin-module-shadowing/) |
| `sys.path` manipulation for imports | Hand-rolled `sys.path.insert(0, ...)` hacks make import resolution order-dependent and fragile vs. proper packaging | Yes | nothing automated; `src`-layout + editable install is the fix | [packaging.python.org src vs flat](https://packaging.python.org/en/latest/discussions/src-layout-vs-flat-layout/) |
| `except Exception` does NOT swallow `KeyboardInterrupt`/`SystemExit` | Both inherit from `BaseException`, not `Exception`, by design | Yes — common belief that it swallows them is **false**; the real footgun is bare `except:` | CodeQL `py/catch-base-exception` catches the *actually* dangerous bare/`BaseException` catch | [CodeQL py-catch-base-exception](https://codeql.github.com/codeql-query-help/python/py-catch-base-exception/) |
| Broad `except:`/`except BaseException` swallows Ctrl-C and exit | Bare except and explicit `except BaseException` DO catch `KeyboardInterrupt`/`SystemExit` | Yes | `ruff` BLE001 (`blind-except`), CodeQL `py/catch-base-exception` | [ruff BLE001](https://docs.astral.sh/ruff/rules/blind-except/) |
| `finally` swallowing exceptions | A `return`/`break`/`continue`/new exception inside `finally` silently discards the exception in flight | Yes | `ruff` B012 (`jump-statement-in-finally`) | [ruff B012](https://docs.astral.sh/ruff/rules/jump-statement-in-finally/) |
| Generator cleanup and `GeneratorExit` | Not letting a generator run to completion or catching-and-swallowing `GeneratorExit` breaks `close()`/`with`-based cleanup | Yes | `ruff` PLE0704-adjacent none specific; runtime `RuntimeError: generator ignored GeneratorExit` | [PEP 342 / generator docs](https://docs.python.org/3/reference/expressions.html#generator-iterator-methods) |
| `contextlib.suppress` over-broad | Wrapping too much code or too broad an exception type suppresses unrelated failures silently | Yes | nothing automated (reviewer judgment only) | [contextlib docs](https://docs.python.org/3/library/contextlib.html#contextlib.suppress) |
| `functools.lru_cache` on instance methods | Caches `self` as part of the key, keeping every decorated instance alive for the cache's lifetime (memory leak) | Yes, documented in the FAQ the decorator links to but not in the decorator's own warning box | nothing automated; `ruff` has no rule for this | [functools lru_cache docs](https://docs.python.org/3/library/functools.html#functools.lru_cache) |
| Class-attribute vs instance-attribute mutation | Mutating a class-level mutable default (e.g. `class C: items = []`) mutates it for every instance | Yes | nothing automated | [Python data model docs](https://docs.python.org/3/reference/datamodel.html) |
| `super()`/MRO surprises with multiple inheritance | C3 linearization order is not always the intuitive left-to-right order; `super()` calls can silently skip a sibling | Yes | nothing automated | [Python docs: super()](https://docs.python.org/3/library/functions.html#super) |
| `@staticmethod`/`@classmethod` inheritance surprises | Overriding a `@classmethod` in a subclass still receives the subclass as `cls`, changing behavior of factory patterns | Yes | nothing automated | [Python docs: classmethod](https://docs.python.org/3/library/functions.html#classmethod) |
| `__slots__` + multiple inheritance / mutable-default interaction | Multiple non-empty `__slots__` bases conflict; `dataclass(slots=True)` cannot combine with `field(default_factory=...)` defaults defined via class body in older versions | Partly — slots+default_factory interaction has been fixed by 3.10's proper dataclass slots support, but multiple-inheritance slot conflicts remain | nothing automated; surfaces as `TypeError` at class creation | [dataclasses docs](https://docs.python.org/3/library/dataclasses.html) |
| `dataclass` mutable default field | `field: list = []` in a dataclass raises `ValueError` at class definition (dataclasses actively reject this, unlike plain functions) | Yes — actively rejected, arguably *safer* than the function version | Enforced by `dataclasses` itself at class-creation time (`ValueError: mutable default <class 'list'> for field ... is not allowed`) | [dataclasses field() docs](https://docs.python.org/3/library/dataclasses.html#dataclasses.field) |
| `dataclass(eq=True, frozen=False)` unhashable-by-default | Non-frozen dataclasses get `__eq__` but `__hash__` set to `None`, silently breaking use as dict keys/set members | Yes | nothing automated; surfaces as `TypeError: unhashable type` | [dataclasses docs](https://docs.python.org/3/library/dataclasses.html) |
| `Enum` identity across module reload | Reloading a module containing an `Enum` recreates distinct member objects that fail identity/equality checks against previously imported references | Yes | nothing automated | [enum HOWTO](https://docs.python.org/3/howto/enum.html) |
| Blocking call inside `async def` | A sync call (file I/O, `time.sleep`, `requests`) inside a coroutine blocks the whole event loop, not just that task | Yes | `flake8-async` (`ASYNC` rules in ruff), Home Assistant's own runtime blocking-call detector | [HA asyncio_blocking_operations](https://developers.home-assistant.io/docs/asyncio_blocking_operations/) |
| Forgetting `await` | Calling a coroutine function without `await` just creates a coroutine object and does nothing; no exception at call site | Yes | `ruff` RUF006/PLE1142-adjacent, `pyright`/`mypy` reportUnusedCoroutine, runtime `RuntimeWarning: coroutine was never awaited` | [asyncio-task docs](https://docs.python.org/3/library/asyncio-task.html) |
| Fire-and-forget `asyncio.create_task()` garbage collected mid-flight | Event loop holds only a weak reference; if nothing else references the Task, it can vanish before completion | Yes — still an open footgun; docs now warn explicitly | Documented mitigation only (keep a strong ref in a set + `add_done_callback` to discard); no lint | [asyncio-task docs task creation warning](https://docs.python.org/3/library/asyncio-task.html) |
| `asyncio.gather()` default swallows sibling cancellation semantics | With default `return_exceptions=False`, first exception propagates but sibling tasks are NOT automatically cancelled unless you do so yourself | Yes | nothing automated; use `asyncio.TaskGroup` (3.11+) instead | [asyncio-task docs](https://docs.python.org/3/library/asyncio-task.html) |
| Catching `asyncio.CancelledError` and not re-raising | Cancellation is delivered as an exception; swallowing it breaks the cancellation contract (task looks alive but the caller thinks it stopped) | Yes | nothing automated; `flake8-async` ASYNC102/ASYNC120-family rules | [asyncio-task cancellation docs](https://docs.python.org/3/library/asyncio-task.html) |
| `run_in_executor` with thread-unsafe libraries | Offloading blocking work to a thread pool silently reintroduces thread-safety bugs for libraries not designed for concurrent use | Yes | nothing automated | [asyncio eventloop docs](https://docs.python.org/3/library/asyncio-eventloop.html#asyncio.loop.run_in_executor) |
| GIL removal changes correctness assumptions | Code relying on the GIL for implicit atomicity (e.g. `x += 1` "seems" atomic) breaks under free-threaded builds | 3.14: free-threading is now **supported** (PEP 779), not just experimental, but GIL build is still default | nothing automated; `python -VV` / `sys._is_gil_enabled()` at runtime | [free-threading howto](https://docs.python.org/3/howto/free-threading-python.html) |
| `multiprocessing` fork start method + threads | `fork()` after threads have started can deadlock or corrupt state; unsafe on macOS, deprecated-with-warning on POSIX since 3.12 | **Changed in 3.14**: default start method on POSIX moved from `fork` to `forkserver` | `DeprecationWarning` raised by `os.fork()` when multiple threads detected (3.12+) | [multiprocessing start-methods docs](https://docs.python.org/3/library/multiprocessing.html#contexts-and-start-methods) |
| `subprocess` pipe deadlock without `communicate()` | Child fills the OS pipe buffer for stdout/stderr and blocks; parent using `wait()`/`.stdout.read()` directly deadlocks | Yes, explicitly documented warning, unchanged | nothing automated; must use `communicate()` or `timeout=` | [subprocess docs](https://docs.python.org/3/library/subprocess.html) |
| `subprocess.run` missing `timeout=` | A hung child process hangs the calling test/process indefinitely | Yes | nothing automated (bandit has no direct rule; project convention only) | [subprocess docs](https://docs.python.org/3/library/subprocess.html) |
| `shell=True` with unsanitized input | Enables shell metacharacter injection when args are interpolated into a string | Yes | `bandit` B602/B605-B609 (subprocess-popen-with-shell-equals-true) | [bandit subprocess checks](https://bandit.readthedocs.io/) |
| Blocking synchronous subprocess call inside an async event loop | `Popen.wait()`/`.communicate()` called directly (not via executor) inside a coroutine blocks the entire loop — real production incident | Yes | nothing automated; caught only by load/chaos testing or manual review | [LiteLLM Prisma reconnect incident](https://docs.litellm.ai/blog/prisma-reconnect-blocking-incident) |
| `tempfile.mktemp()` race condition | Returns a name only; file can be created by another process/attacker between name generation and open | Deprecated since 2.3, still present, still exploitable if used | `bandit` B306 (`mktemp_q`) | [tempfile docs](https://docs.python.org/3/library/tempfile.html) |
| `tarfile.extractall()` path traversal | Archive members with `../` sequences can write outside the target directory (CVE-2007-4559) | **Fixed in 3.12**: `filter=` parameter added (PEP 706); default changes to `'data'` filter in 3.14 per deprecation schedule | `bandit` B202 (`tarfile_unsafe_members`) | [PEP 706](https://peps.python.org/pep-0706/) |
| `zipfile` decompression / zip-bomb DoS | No built-in cap on uncompressed size; a small archive can expand to exhaust memory/disk | Yes — CVE-2019-9674 explicitly says "no plan to fix," caller must validate | nothing automated; must check `ZipInfo.file_size` before extracting | [bpo-36462 CVE-2019-9674](https://bugs.python.org/issue36462) |
| XML entity expansion (billion laughs) | stdlib `xml.etree`/`xml.dom.minidom`/`xml.sax` are vulnerable to entity-expansion DoS by default | Yes, stdlib intentionally does not fix this; must use `defusedxml` | `bandit` B313-B320 (`xml_*` family) | [Python docs: XML vulnerabilities](https://docs.python.org/3/library/xml.html#xml-vulnerabilities) |
| `pickle.load` on untrusted data | Deserializing a pickle stream can execute arbitrary code via `__reduce__` | Yes, unchanged and explicitly documented as unsafe | `bandit` B301 (`pickle`) | [pickle docs security warning](https://docs.python.org/3/library/pickle.html) |
| `yaml.load()` without a safe loader | Default/`FullLoader` can still be coerced into object construction gadgets (CVE-2017-18342, CVE-2020-1747/14343) | Yes — `safe_load` still not the default in PyYAML; must opt in | `bandit` B506 (`yaml_load`) | [PyYAML load deprecation wiki](https://github.com/yaml/pyyaml/wiki/PyYAML-yaml.load(input)-Deprecation) |
| `eval`/`exec` on untrusted input | Arbitrary code execution | Yes | `bandit` B307 (`eval`), B102 (`exec_used`) | [bandit eval/exec](https://bandit.readthedocs.io/) |
| `hashlib.md5()`/`sha1()` for security purposes | Both are cryptographically broken for collision resistance; still fine for non-security checksums | Yes | `bandit` B303/B324 (weak hash) | [hashlib docs](https://docs.python.org/3/library/hashlib.html) |
| `random` module used for security tokens | `random` is a Mersenne Twister — not cryptographically secure, seed-predictable | Yes, explicit doc warning: "should not be used for security purposes" | `bandit` B311 (`random`) | [secrets docs](https://docs.python.org/3/library/secrets.html) |
| Missing TLS verification (`verify=False`) | Disables certificate validation, opening MITM | Yes | `bandit` B501 (`request_with_no_cert_validation`) | [requests docs](https://requests.readthedocs.io/) |
| Symlink following during file operations | `shutil`/`tarfile`/`zipfile` extraction can follow or create symlinks that escape the target directory | Partly mitigated by 3.12 tarfile `filter=` for tar; zipfile still unguarded | `bandit` has no direct rule for this specific pattern | [PEP 706](https://peps.python.org/pep-0706/) |
| Terminal ANSI escape-sequence injection | Printing untrusted strings containing ANSI control sequences can rewrite terminal titles, hide text, or (via some emulators) worse | Yes, general terminal-emulator-level issue, not Python-specific | nothing automated in stdlib; must sanitize before printing to a TTY | [dgl.cx ANSI terminal security 2023](https://dgl.cx/2023/09/ansi-terminal-security) |
| `pytest` `tmpdir`/`py.path.local` legacy fixture | Non-pathlib legacy fixture; being phased toward removal via `legacypath` plugin | Yes, `tmpdir` still works but is superseded | `flake8-pytest-style` PT-family rule for tmpdir→tmp_path migration | [pytest tmp_path how-to](https://docs.pytest.org/en/stable/how-to/tmp_path.html) |
| `unittest.mock.patch` targeting wrong namespace | Patching where a name is *defined* instead of where it's *imported and used* leaves the real object untouched | Yes | nothing automated (some IDEs autospec-check, but no lint) | [mock patch docs](https://docs.python.org/3/library/unittest.mock.html) |
| pytest-xdist shared mutable/global state races | Parallel workers race over shared fixtures, fixed ports, hardcoded temp paths, or global module state | Yes | nothing automated; must design fixtures for per-worker isolation (`PYTEST_XDIST_WORKER`) | [pytest-xdist docs](https://pytest-xdist.readthedocs.io/) |
| `coverage.py` 100% gamed via `# pragma: no cover` | Excluding lines from measurement inflates the reported percentage without adding tests | Yes | nothing automated stops pragma abuse; only code review / diff-cover on new pragmas | [coverage.py excluding docs](https://coverage.readthedocs.io/en/latest/excluding.html) |
| Coverage measures import, not behavior | 100% line coverage can still hide untested branches, exception paths and edge cases | Yes | `coverage.py` branch coverage (`--branch`) narrows but doesn't close this gap | [coverage.py config docs](https://coverage.readthedocs.io/en/latest/config.html) |
| Flat layout hides packaging bugs | Running tests against the checked-out source tree (not the built/installed package) can pass while the shipped wheel is broken (missing files, wrong package name) | Yes | nothing automated; `src` layout + editable install forces the real import path | [packaging.python.org src-vs-flat](https://packaging.python.org/en/latest/discussions/src-layout-vs-flat-layout/) |
| PyPI typosquatting / dependency confusion | Malicious packages with names similar to popular ones (or matching an internal private package name) get installed by mistake | Yes, active and ongoing (899/1042 typosquats registered in a single 2024 campaign) | nothing automated at install time; requires lockfile pinning + private index priority config | [Phylum PyPI typosquatting postmortem](https://blog.phylum.io/a-pypi-typosquatting-campaign-post-mortem/) |
| `setup.py` arbitrary code execution at install time | Any `pip install` of a package with a `setup.py` executes attacker-controlled Python during resolution/build | Yes, unless the package ships only a wheel (no sdist) or uses PEP 517 static metadata | nothing automated; prefer wheels / `--only-binary` for untrusted sources | [PyPA packaging guide](https://packaging.python.org/) |

**Nothing-caught count: 32 of 61 catalogue rows have a "nothing"-prefixed caught-by column** (i.e., no lint/type-checker/static rule catches them at all — only runtime behavior, documentation discipline, or manual review does). A further 3 rows (generator/GeneratorExit, fire-and-forget create_task, symlink following) are caught by nothing automated but were phrased without the literal word "nothing."

## Recurring reviewer objections

| Objection (quoted) | Repo | Thread | Generalizes? |
|---|---|---|---|
| "We're leaking the cache path here, I don't think we need to show that path." | astral-sh/uv | [uv#18214](https://github.com/astral-sh/uv/pull/18214) | Yes — never surface internal filesystem paths in user-facing output/errors; applies directly to CLI-wrapping test harnesses (shape 1) that assert on stderr |
| "We'll want to use an error case for this to avoid panicking if someone has a bad keyring executable" | astral-sh/uv | [uv#2254](https://github.com/astral-sh/uv/pull/2254) | Yes — external-process/credential-helper failures must become typed errors, never crash the caller; generalizes to Python `subprocess` wrappers around external tools |
| "Should we pick a lower limit? 100 can help overflow Windows' 256 MAX_PATH limit" | astral-sh/uv | [uv#18214](https://github.com/astral-sh/uv/pull/18214) | Yes — path-length/portability assumptions baked into generated paths need a Windows check; relevant to any stdlib-only cross-platform tool (shape 4) |
| "Could you evaluate please? Adding stuff to the global namespace sounds risky." | python-attrs/attrs | [attrs#627](https://github.com/python-attrs/attrs/pull/627) | Yes — mutating/injecting into global module namespace at import or call time is treated as inherently risky by maintainers, independent of language |
| "I'm curious why we're overriding `__new__` instead of `__init__` here?" | encode/httpx | [httpx#3022](https://github.com/encode/httpx/pull/3022) | Yes — unusual use of `__new__` over `__init__` is a red flag reviewers reflexively question; worth a heuristic ("does this really need `__new__`?") |
| "We should be using `typing.Optional[ssl.SSLContext]` here. (Yes it's a bit subtle)" | encode/httpx | [httpx#3022](https://github.com/encode/httpx/pull/3022) | Partial — type-hint precision on nullable security-relevant parameters (TLS context) matters more than nullable params elsewhere; narrowly generalizes to typed-library shape (2) |
| "Another important question is whether we need to deprecate previous arguments or simply delete them." | encode/httpx | [httpx#3022](https://github.com/encode/httpx/pull/3022) | Yes — breaking a public API silently vs. via `DeprecationWarning` is a recurring maintainer gate on any typed library (shape 2) |
| "It seems that this `allow` isn't needed" / "This `allow` seems unnecessary" (repeated across the same PR) | astral-sh/ruff | [ruff#9266](https://github.com/astral-sh/ruff/pull/9266) | Yes (cross-language) — suppressions/ignores added defensively but never actually needed accumulate as dead noise; matches Python's `# noqa`/`# type: ignore` without a reason |
| "Would you mind adding a comment explaining why `previous_blank_lines` is set to ... ? I don't understand the reason and find it confusing" | astral-sh/ruff | [ruff#9266](https://github.com/astral-sh/ruff/pull/9266) | Yes — non-obvious invariants (especially ones backing an `unwrap`/assumption-of-safety) must be commented at the point of the assumption, not just at the call site |

## Candidate topics

| Topic (as a question) | Why it matters | Source | Already covered by an enable-able lint? | Priority / shape |
|---|---|---|---|---|
| Does this async function ever call something blocking without `run_in_executor`/`asyncio.to_thread`? | Freezes the entire event loop, not just one task — real production outage (LiteLLM) | [litellm incident](https://docs.litellm.ai/blog/prisma-reconnect-blocking-incident) | Partially — `flake8-async`/ruff `ASYNC` rules catch some known-blocking stdlib calls, not arbitrary sync libs | **High** — shape 2 (asyncio SDK), shape 3 (subprocess-driving harness under async runners) |
| Does every `asyncio.create_task()` result get stored in a strong reference collection? | Undocumented-feeling but explicitly-warned-about GC of in-flight tasks | [asyncio-task docs](https://docs.python.org/3/library/asyncio-task.html) | No | **High** — shape 2, shape 3 |
| Does every `subprocess.Popen`/`run` call that pipes stdout+stderr use `communicate()` or a `timeout=`? | Deadlock and test-hang risk is the single most load-bearing failure mode for shape 1 | [subprocess docs](https://docs.python.org/3/library/subprocess.html) | No | **Highest** — shape 1 (pytest black-box CLI harness) |
| Is `shell=True` ever used with interpolated/untrusted strings? | Shell injection | [bandit subprocess checks](https://bandit.readthedocs.io/) | Yes — bandit B602-B609 | **High** — shape 1, shape 3 |
| Does the test suite assert on raw captured output that mixes stdout/stderr order, or ANSI codes from a non-TTY run? | TTY-detection changes program output; interleaving is nondeterministic; brittle golden-output tests | scout's own subprocess-testing corpus knowledge | No | **High** — shape 1 |
| Does a CLI-driving test leave orphaned child processes or process groups on timeout/failure? | Zombie processes leak across the ~8 replicated repos' CI | scout's own corpus knowledge | No | **High** — shape 1 |
| Is `pexpect` output assumed byte-identical across CI vs local TTY? | pty flakiness, encoding-locale drift | scout's own corpus knowledge | No | **Medium** — shape 1 |
| Does any fixture leak environment variables or `cwd` across tests? | Cross-test contamination, order-dependent flakiness | [pytest flaky tests explanation](https://docs.pytest.org/en/stable/explanation/flaky.html) | No | **High** — shape 1, shape 3 |
| Is the test suite pytest-xdist-safe (no fixed ports, shared temp paths, or global state)? | Parallel CI runs expose races invisible in serial runs | [pytest-xdist docs](https://pytest-xdist.readthedocs.io/) | No | **High** — shape 1, shape 3 |
| Does `unittest.mock.patch(...)` target the *importer's* namespace, not the definer's? | The single most common mocking bug, silently makes tests pass while not testing anything | [mock patch docs](https://docs.python.org/3/library/unittest.mock.html) | No | **High** — shape 1, shape 3 (heavy subprocess/API mocking) |
| Is a `fail_under=100` coverage gate accompanied by review discipline on new `# pragma: no cover` lines? | 100% coverage is easy to game and cheap to rubber-stamp | [coverage.py excluding docs](https://coverage.readthedocs.io/en/latest/excluding.html) | No (diff-cover style tools exist but aren't stdlib) | **High** — shape 2 (ocx-sdk-python, fail_under=100 explicitly) |
| Are Sybil doctests actually executed in CI, or just present in docstrings? | Doctests silently bit-rot if not wired into the test runner | project-supplied context (Sybil doctests) | No | **Medium** — shape 2 |
| Does async code under test correctly propagate/re-raise `asyncio.CancelledError`? | Swallowing cancellation breaks the whole cooperative-cancellation contract | [asyncio-task docs](https://docs.python.org/3/library/asyncio-task.html) | No | **High** — shape 2 |
| Are `asyncio.gather()` calls using `return_exceptions=` deliberately, or should this be a `TaskGroup`? | Silent swallowing of sibling task failures is easy to introduce by omission | [asyncio-task docs](https://docs.python.org/3/library/asyncio-task.html) | No | **Medium-High** — shape 2 |
| Does the package use `src/` layout, and do tests run against the installed/editable package rather than the raw checkout? | Flat layout can hide real packaging bugs that only surface for end users | [packaging.python.org](https://packaging.python.org/en/latest/discussions/src-layout-vs-flat-layout/) | No | **Medium** — shape 2, shape 3 |
| Are dependencies pinned via a lockfile, and does CI fail on lockfile/manifest divergence? | Transitive drift causes "works on my machine" and supply-chain exposure | [Phylum typosquatting postmortem](https://blog.phylum.io/a-pypi-typosquatting-campaign-post-mortem/) | No | **Medium** — all shapes with dependencies |
| Does any code path call `pickle.load`/`yaml.load`(unsafe)/`eval`/`exec` on data that could ever be attacker-influenced? | RCE-class vulnerabilities, real CVEs, `bandit` catches all four directly | [bandit rules](https://bandit.readthedocs.io/) | Yes — bandit B301/B506/B307/B102 | **Highest** — all shapes, security gate |
| Does `tarfile`/`zipfile` extraction of any externally-sourced archive validate paths and size before writing? | Path traversal (CVE-2007-4559) and zip-bomb DoS (CVE-2019-9674, "no plan to fix") | [PEP 706](https://peps.python.org/pep-0706/), [bpo-36462](https://bugs.python.org/issue36462) | Partial — bandit B202 catches tarfile only, not zip-bomb sizing | **High** — shape 3/4 (CLI tools that unpack registry/OCI artifacts — directly relevant to this project's own domain) |
| Is `hashlib.md5`/`sha1` used anywhere a security property (not just a checksum) is implied? | Weak-hash-for-security is a classic false-sense-of-integrity bug | [bandit weak hash rules](https://bandit.readthedocs.io/) | Yes — bandit B303/B324 | **Medium** | 
| Is `random` (not `secrets`) used to generate any token, password, or nonce? | Predictable secrets | [secrets docs](https://docs.python.org/3/library/secrets.html) | Yes — bandit B311 | **High** — any shape generating credentials/session tokens |
| Does the code ever print/log untrusted strings directly to a terminal without stripping control sequences? | ANSI escape injection into terminal output/logs | [dgl.cx ANSI terminal security](https://dgl.cx/2023/09/ansi-terminal-security) | No | **Medium** — shape 1, shape 3 (CLI output capture/printing) |
| Does `datetime.utcnow()` or another naive-datetime call appear anywhere, given it's deprecated and scheduled for removal? | Silent removal breakage risk plus historical naive/aware bugs | [utcnow deprecation](https://blog.miguelgrinberg.com/post/it-s-time-for-a-change-datetime-utcnow-is-now-deprecated) | Yes — ruff DTZ003 (flake8-datetimez) | **Medium** — all shapes |
| Does the codebase assume `multiprocessing`'s default start method is `fork`? | 3.14 changed the POSIX default to `forkserver`; fork-reliant code (shared globals, unpicklable objects) breaks silently on upgrade | [multiprocessing docs](https://docs.python.org/3/library/multiprocessing.html#contexts-and-start-methods) | No | **Medium** — shape 3/4 if multiprocessing is used |
| Is any code relying on GIL-provided atomicity that would break under a free-threaded build? | 3.14 makes free-threading a *supported*, not just experimental, build target | [free-threading howto](https://docs.python.org/3/howto/free-threading-python.html) | No | **Low-Medium** — forward-looking, not urgent for this project's current shapes |
| Does `@functools.lru_cache` decorate an instance method? | Silent memory leak by keeping `self` alive for the cache's lifetime | [functools docs](https://docs.python.org/3/library/functools.html#functools.lru_cache) | No | **Medium** — shape 2, shape 3 |
| Does a non-frozen `@dataclass` get used as a dict key or set member? | `eq=True` without `frozen=True`/explicit `__hash__` makes the class unhashable at runtime, not at definition time | [dataclasses docs](https://docs.python.org/3/library/dataclasses.html) | No | **Medium** — shape 2, shape 4 |
| Does a broad `except Exception`/bare `except:` wrap a subprocess call, hiding a hung/killed child's real error? | Compounds with the subprocess-deadlock failure mode — masks the actual root cause in CLI-testing shape | [ruff BLE001](https://docs.astral.sh/ruff/rules/blind-except/) | Yes — ruff BLE001 | **High** — shape 1, shape 3 |
| Is `contextlib.suppress(Exception)` (broad) used anywhere instead of a narrow, named exception? | Silently absorbs unrelated failures, mirrors bare-except risk but reads as "clean" code | [contextlib docs](https://docs.python.org/3/library/contextlib.html#contextlib.suppress) | No | **Medium** | 
| Does test isolation depend on execution order (module-scoped fixtures mutated by an earlier test)? | Order-dependent flakiness, invisible until `pytest-randomly` or `-p no:randomly` toggling | [pytest flaky docs](https://docs.pytest.org/en/stable/explanation/flaky.html) | Partial — `pytest-randomly` surfaces it but doesn't prevent it | **High** — shape 1, shape 3 |
| Are internal file paths, tokens, or stack traces ever echoed back into CLI output or error messages? | Reviewer objection pattern from uv (#18214) — path/secret leakage into user-facing output | [uv#18214](https://github.com/astral-sh/uv/pull/18214) | No | **Medium** — shape 1, shape 4 (this project's own CLI-tool domain) |
| Does a generated path/filename ever get checked for Windows `MAX_PATH` (260 char) limits? | uv reviewer objection; breaks on Windows silently | [uv#18214](https://github.com/astral-sh/uv/pull/18214) | No | **Low-Medium** — shape 4 (cross-platform stdlib tools) |
| Is `__new__` overridden where `__init__` would suffice? | Reviewer-flagged red flag in httpx; usually signals a misunderstanding of object construction | [httpx#3022](https://github.com/encode/httpx/pull/3022) | No | **Low** — general code-review heuristic |
| Does a public API removal skip a `DeprecationWarning` period? | Recurring maintainer gate across typed libraries (httpx) — directly relevant to ocx-sdk-python's public contract | [httpx#3022](https://github.com/encode/httpx/pull/3022) | No | **Medium** — shape 2 |
| Does an added `# noqa`/`# type: ignore`/lint-`allow` actually still trigger the suppressed rule? | Ruff-repo pattern: unnecessary suppressions accumulate as noise and hide when a rule starts actually firing | [ruff#9266](https://github.com/astral-sh/ruff/pull/9266) | Partial — `ruff --select RUF100` / `unused-noqa` catches literal dead noqa comments | **Medium** — all shapes |
| Does `zoneinfo`/timezone-aware arithmetic get used near any DST boundary logic instead of naive local time? | Silent wrong-by-an-hour bugs that only reproduce twice a year | [zoneinfo docs](https://docs.python.org/3/library/zoneinfo.html) | No | **Low-Medium** |

## Sources

| URL | What it is | Date/era | Why worth reading |
|---|---|---|---|
| [docs.python.org/3/library/subprocess.html](https://docs.python.org/3/library/subprocess.html) | Primary stdlib docs | current (3.14) | Documents the pipe-deadlock warning verbatim; load-bearing for shape 1 |
| [docs.python.org/3/library/asyncio-task.html](https://docs.python.org/3/library/asyncio-task.html) | Primary stdlib docs | current (3.14) | create_task GC warning, cancellation, gather semantics |
| [docs.python.org/3/library/multiprocessing.html#contexts-and-start-methods](https://docs.python.org/3/library/multiprocessing.html#contexts-and-start-methods) | Primary stdlib docs | current (3.14) | Confirms the 3.14 forkserver-default change (gh-84559) |
| [docs.python.org/3/howto/free-threading-python.html](https://docs.python.org/3/howto/free-threading-python.html) | Primary stdlib docs | current (3.14) | Confirms free-threading is supported-not-default in 3.14 |
| [docs.python.org/3/library/functools.html#functools.lru_cache](https://docs.python.org/3/library/functools.html#functools.lru_cache) | Primary stdlib docs | current | Confirms the instance-method caching note and its FAQ pointer |
| [docs.python.org/3/library/gc.html](https://docs.python.org/3/library/gc.html) | Primary stdlib docs | current | `__del__`/resurrection semantics via tp_finalize |
| [docs.python.org/3/library/tempfile.html](https://docs.python.org/3/library/tempfile.html) | Primary stdlib docs | current | mktemp race-condition deprecation notice |
| [docs.python.org/3/library/pickle.html](https://docs.python.org/3/library/pickle.html) | Primary stdlib docs | current | Explicit "never unpickle untrusted data" security warning |
| [docs.python.org/3/library/secrets.html](https://docs.python.org/3/library/secrets.html) | Primary stdlib docs | current | secrets-vs-random security guidance |
| [docs.python.org/3/library/xml.html#xml-vulnerabilities](https://docs.python.org/3/library/xml.html#xml-vulnerabilities) | Primary stdlib docs | current | Confirms stdlib XML parsers are intentionally left vulnerable to entity expansion |
| [peps.python.org/pep-0706](https://peps.python.org/pep-0706/) | PEP text | accepted, landed 3.12 | Primary source for the tarfile.extractall CVE-2007-4559 fix and its filter= mechanics |
| [bugs.python.org/issue36462](https://bugs.python.org/issue36462) (CVE-2019-9674) | CPython bug tracker | 2019, still open/wontfix | Primary source that zipfile's zip-bomb issue has "no plan to fix" |
| [bugs.python.org/issue35615](https://bugs.python.org/issue35615) | CPython bug tracker | ongoing | dict-changed-size-during-iteration edge case via GC/WeakValueDictionary |
| [github.com/python/cpython/issues/79031](https://github.com/python/cpython/issues/79031) | CPython issue tracker | historical, still referenced | Origin of the SyntaxWarning for `is` with a literal |
| [docs.astral.sh/ruff/rules/](https://docs.astral.sh/ruff/rules/) | Ruff rule reference | current | Canonical source for exact rule codes cited throughout the catalogue |
| [bandit.readthedocs.io](https://bandit.readthedocs.io/) | Bandit security-linter docs | current | Canonical source for B-series security rule codes cited throughout |
| [github.com/astral-sh/uv/pull/18214](https://github.com/astral-sh/uv/pull/18214) | Merged PR review thread | 2026 | Path-leakage and Windows MAX_PATH reviewer objections |
| [github.com/astral-sh/uv/pull/2254](https://github.com/astral-sh/uv/pull/2254) | Merged PR review thread | 2024 | External-process (keyring) error-handling reviewer objection |
| [github.com/astral-sh/ruff/pull/9266](https://github.com/astral-sh/ruff/pull/9266) | Merged PR review thread | 2024 | Dead-suppression and undocumented-invariant reviewer objections |
| [github.com/python-attrs/attrs/pull/627](https://github.com/python-attrs/attrs/pull/627) | Merged PR review thread | historical | "Global namespace mutation is risky" maintainer objection |
| [github.com/encode/httpx/pull/3022](https://github.com/encode/httpx/pull/3022) | Merged PR review thread | 2024 | `__new__`-vs-`__init__` and deprecate-vs-delete API objections |
| [developers.home-assistant.io/docs/asyncio_blocking_operations](https://developers.home-assistant.io/docs/asyncio_blocking_operations/) | Flagship project developer docs | current | Codifies the exact list of stdlib calls considered "blocking" in an event loop, with required alternative |
| [docs.litellm.ai/blog/prisma-reconnect-blocking-incident](https://docs.litellm.ai/blog/prisma-reconnect-blocking-incident) | Postmortem | 2026 | Real production outage: sync `subprocess.Popen.wait()` inside async code froze the event loop and triggered k8s SIGKILLs |
| [blog.phylum.io/a-pypi-typosquatting-campaign-post-mortem](https://blog.phylum.io/a-pypi-typosquatting-campaign-post-mortem/) | Postmortem | 2024 | Supply-chain/typosquatting incident with concrete numbers (899/1042 typosquats registered) |
| [blog.miguelgrinberg.com/post/it-s-time-for-a-change-datetime-utcnow-is-now-deprecated](https://blog.miguelgrinberg.com/post/it-s-time-for-a-change-datetime-utcnow-is-now-deprecated) | Practitioner blog, well-cited | 2023-2024 | Clear practitioner explanation of the utcnow deprecation and migration path |
| [nedbatchelder.com/blog/202405/one_way_to_fix_python_circular_imports.html](https://nedbatchelder.com/blog/202405/one_way_to_fix_python_circular_imports.html) | Practitioner blog (coverage.py maintainer) | 2024 | High-signal circular-import mechanics from a CPython-adjacent authority |
| [packaging.python.org/en/latest/discussions/src-layout-vs-flat-layout](https://packaging.python.org/en/latest/discussions/src-layout-vs-flat-layout/) | Official PyPA packaging guide | current | Primary source for why flat layout hides packaging bugs that src layout catches |
| [docs.pytest.org/en/stable/explanation/flaky.html](https://docs.pytest.org/en/stable/explanation/flaky.html) | Official pytest docs | current | Primary taxonomy of flaky-test causes |
| [docs.pytest.org/en/stable/how-to/tmp_path.html](https://docs.pytest.org/en/stable/how-to/tmp_path.html) | Official pytest docs | current | tmp_path vs legacy tmpdir status |
| [pytest-xdist.readthedocs.io](https://pytest-xdist.readthedocs.io/) | Official pytest-xdist docs | current | Parallel-test isolation requirements (`PYTEST_XDIST_WORKER`) |
| [coverage.readthedocs.io/en/latest/excluding.html](https://coverage.readthedocs.io/en/latest/excluding.html) | Official coverage.py docs | current | Mechanics of `# pragma: no cover` and how it's gamed against `fail_under` |
| [dgl.cx/2023/09/ansi-terminal-security](https://dgl.cx/2023/09/ansi-terminal-security) | Independent security research, found 10 CVEs | 2023 | Deep, well-cited treatment of terminal escape-injection risk relevant to CLI output |
