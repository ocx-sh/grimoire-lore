# Reference

Setting the aforementioned flag to a truthy boolean value activates the
strict verification subsystem, which subsequently interposes a rejection
pathway for any artifact whose provenance attestation cannot be
cryptographically substantiated against the configured trust anchor.
