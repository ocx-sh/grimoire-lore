---
title: "Fleet fix list — concrete, already-verified Python defects"
agent: general-purpose (sonnet)
model: claude-sonnet-5
scope: synthesis of every artifact under .agents/research/python-audit/, .agents/research/python-topic-map/, and .agents/research/python-typing/annotations-evaluation.md
cutoff: >
  Original pass read at 2026-08-23 00:22 CEST. Directory listing taken first, per
  instructions: python-audit/ had 9 files (config-inventory, exemplar-patterns,
  existing-rules-ledger, harness-shape, lint-yield, pyright-triage, shipped-python,
  tooling-posture, version-floor.md) — all present and read in full, including the
  three this same agent authored earlier in this session (harness-shape, lint-yield,
  pyright-triage). python-topic-map/ had 10 files, header/frontmatter-skimmed on that
  pass; python-typing/annotations-evaluation.md read in full. **Correction pass, same
  day**: a header-level skim wrongly excluded `scout-cli-acceptance.md` — its header
  reads as a pure external-corpus scout, but its body reads `pip`/`pytest`/`uv`'s own
  test-infrastructure source directly and cross-references this fleet's own
  `harness-shape.md`/`existing-rules-ledger.md` in its own Sources list, which is the
  signal a header-only read misses. Re-checked the other 7 python-topic-map/ files
  (scout-agent-legibility, scout-canonical, scout-codified, scout-failure,
  scout-practitioner, scout-shifts, sweep-canonical-catalogues) by grepping their full
  bodies (not just headers) for fleet-repo references — none cite this program's own
  audit artifacts or introduce a new fleet-specific already-verified defect; codified-
  reconciled.md and scout-failure-coverage.md were already read in full in the original
  pass, correctly, since their headers stated fleet re-measurement. A new directory,
  `python-subprocess/process-control.md` (not part of the original commission's three
  named directories), was created after the original pass and read in full during the
  correction pass at the commissioning agent's direction, since it corrects and extends
  FX-07 directly.
method: >
  Every row below was checked against its source artifact's own citation, and every
  file:line was independently re-opened and read by this agent (not trusted from the
  source artifact's prose alone) before being included — see the exact commands quoted
  per row. One candidate from the commissioning message did not survive verification as
  written; one was wrongly dropped in the original pass and is corrected in place — see
  "Dropped" at the end for both, in full. Read-only: no file in any audited repository
  (ocx, grimoire, ocx-sdk-python, ocx-mirror-sdk, index, ocx-sion, ocx-soraka,
  ocx-evelynn, ocx-mirror, ocx-mcp, ocx-save, grimoire-duo) was modified to produce this
  file.
---

# Fleet fix list

19 rows survive verification (16 in the original pass, 3 added — FX-17/18/19 — after
`python-subprocess/process-control.md` landed and corrected FX-07's premise; see the
"Dropped" section for that correction in full). Ordered by severity (breaks-now > latent > hygiene), then
by cheapness within a tier. Every row cites the artifact it came from AND the direct
command this agent ran to confirm it before listing it here.

## The table

| ID | Repo / file:line | What's wrong | Proven by | Severity | Fix | Blast radius | Who |
|---|---|---|---|---|---|---|---|
| FX-01 | `ocx/test/pyproject.toml:4` | `requires-python = ">=3.10"`; suite fails to even **collect** on 3.10 or 3.11 | `version-floor.md` §2, live: `uv run --python 3.10 -m pytest --collect-only -q` → `SyntaxError` (PEP 701 nested f-string) + 3 `tomllib`/`datetime.UTC` import errors, 4 errors during collection. Clean only from 3.12. | breaks-now | `requires-python = ">=3.12"` + commit `.python-version` → `3.12` | **8 repos** carry the identical `requires-python = ">=3.10"` line — verified directly: `grep requires-python ocx-sion/test/pyproject.toml ocx-soraka/... ocx-evelynn/... ocx-mirror/... ocx-mcp/... ocx-save/... grimoire-duo/...` all return `>=3.10`. Only `ocx/test` and `grimoire/test` were independently collection-tested against their declared floor; the true floor for the other 6 was not live-verified in any artifact — flagged, not claimed. | mechanical for `ocx/test` itself; a fleet-wide sweep across the other 7 is a rollout decision (see "needs a human decision") |
| FX-02 | `grimoire/test/pyproject.toml:4` | Same shape, different true floor: `>=3.10` declared, fails to collect at 3.10 (`ModuleNotFoundError: tomllib`), clean from 3.11 | `version-floor.md` §2, live: `uv run --python 3.10 -m pytest --collect-only -q` → 6 collection errors, all `tomllib`. Clean at 3.11: `1060 tests collected in 1.25s`. | breaks-now | `requires-python = ">=3.11"` + `.python-version` → `3.11` | `grimoire-duo/test/pyproject.toml` carries the identical `>=3.10` string (verified above) — unknown whether its true floor matches grimoire's 3.11 or ocx's 3.12; not independently collection-tested | mechanical for `grimoire/test`; sibling rollout is a decision |
| FX-03 | `grimoire/test/conftest.py:355`, `tests/test_agents.py:99`, `tests/test_render_clients.py:82,91,110,127,842,846,858,865`, `ocx/test/tests/test_doc_scripts_cast.py:440` | 11 forward-ref annotations name a class that is never bound at module scope (only imported locally, one line inside the annotated function) — `reportUndefinedVariable`/`F821` | `pyright-triage.md` §3 (pyright, both repos) and `annotations-evaluation.md` §4, independently, same 11 sites, both confirmed live: `ruff check --select F821 --no-cache conftest.py` → `F821 Undefined name 'GrimRunner'` at `:355:50`, `Found 1 error`. Re-confirmed this session: `sed -n '353,358p' grimoire/test/conftest.py` shows `def grim(...) -> "GrimRunner":` at line 355, `from src.runner import GrimRunner` at 356. | latent — silent forever under pytest (matches fixtures by name, never evaluates annotations, confirmed by a live reproduction on 3.12 and 3.14 in `annotations-evaluation.md` §4); raises `NameError` the instant anything calls `get_type_hints`/`inspect.signature(eval_str=True)`/adds pydantic-or-similar | Move each local import to module scope (or import the real class under `if TYPE_CHECKING:` and keep `from __future__ import annotations`, which both repos already have) | 2 repos, 11 sites total (1 ocx, 10 grimoire); zero-config detectable — `ruff check --select F821` needs no config file in either repo today | mechanical — an agent can safely move each local import to module scope; `git grep -n 'noqa: F821'` first to catch FX-04's site |
| FX-04 | `ocx/test/tests/test_doc_scripts_cast.py:440` | The existing `# noqa: F821 — forward ref; resolved at runtime` suppression's own claim is false: nothing in this codebase ever resolves it as a type, ever — pytest matches by parameter name only | `annotations-evaluation.md` §4, §Normative-guidance #7. Re-confirmed directly: `sed -n '438,441p' ocx/test/tests/test_doc_scripts_cast.py` → `ocx: "OcxRunner",  # noqa: F821 — forward ref; resolved at runtime` on line 440 (both `pyright-triage.md` and `annotations-evaluation.md` cited slightly different line numbers, 442 and 441 — 440 is the confirmed, directly-read location of the noqa comment itself) | hygiene | Reword the comment: `# noqa: F821 — pytest matches this fixture by parameter name; the annotation is documentation only, never resolved as a type` | 1 site, 1 repo | mechanical, one-line comment edit |
| FX-05 | `grimoire/test/tests/test_fix_installer_txn.py:165`; `test_state_portability.py:49,60`; `test_status.py:357` | 3 hand-written annotations lie about their type — `grim: "object"` (actually `GrimRunner`), `-> object` ×2 (actually `PublishedArtifact`), `-> tuple` (actually a single `GrimRunner`, no tuple involved) — producing 26 of pyright's 76 latent errors | `pyright-triage.md` §2, "hand-written annotations that don't match the real type." Re-confirmed directly: `sed -n '163,166p' test_fix_installer_txn.py` → `def test_env_aliased_root_refuses_instead_of_deleting(grim: "object", ...)`, body immediately does `grim.home`/`grim.env`/`grim.run(...)`; `sed -n '355,379p' test_status.py` → `def _install_deprecated_skill(...) -> tuple:` whose only `return` statement is `return runner` (a single object, never a tuple literal) | latent | `grim: "object"` → `grim: "GrimRunner"`; `_rule_artifact`/`_multifile_rule_artifact` `-> object` → `-> PublishedArtifact`; `_install_deprecated_skill(...) -> tuple:` → `-> GrimRunner:` (import `GrimRunner` at module scope, or under `TYPE_CHECKING` — do not repeat FX-03's mistake while fixing this) | 1 repo, 3 lines, 26 downstream error reports | mechanical |
| FX-06 | `ocx/test/tests/fake_gitlab.py:56` | `class GitLabRoutes:` (no base, no self-type) references `self.lock`/`self.gitlab_ids`/`self.repos`/etc. that live on `FakeForge`, the class it's mixed into (`fake_forge.py:37,217`) — correct at runtime, invisible to pyright, producing 62 of its 154 errors from one class | `pyright-triage.md` §2. Re-confirmed: `grep -n "class GitLabRoutes\|from fake_gitlab import" ocx/test/tests/fake_gitlab.py ocx/test/tests/fake_forge.py` → `fake_gitlab.py:56: class GitLabRoutes:`, `fake_forge.py:37: from fake_gitlab import GitLabRoutes`; no `self:` annotation anywhere in `fake_gitlab.py` (`grep -c "self:" fake_gitlab.py` → 0) | hygiene | Add a `TYPE_CHECKING`-guarded self-type: `if TYPE_CHECKING: from fake_forge import FakeForge` then annotate every method `self: "FakeForge"`, matching the `_Handler` self-type already used one file over (`fake_gitlab.py:33`, `if TYPE_CHECKING: from fake_forge import _Handler`) | 1 repo, 1 class, 62 downstream reports | mechanical, but the pattern choice (self-type vs. accept as documented idiom) is a house-style call — flagged below |
| FX-07 | `ocx/test/src/runner.py:120` (`OcxRunner.run`), `grimoire/test/src/runner.py:105` (`GrimRunner.run`); `ocx/.github/workflows/verify-deep.yml`, `grimoire/.github/workflows/verify-deep.yml`, `test/taskfile.yml` in both repos | **Revised after `python-subprocess/process-control.md` corrected the premise** (see "Explicitly not a defect" below): the harness's chokepoint has no per-call `timeout=`, but that alone matches pip/pytest/uv, not a gap. What *is* a live gap, proven by reading the actual CI files: neither `acceptance-tests` job sets `timeout-minutes` (GitHub's 360-minute default/hard-max applies); CI's real invocation (`task test -- --junit-xml=...`) silently drops the taskfile's own `-v` default via Task's `default`-filter templating, so no test name prints before a hang; and `Upload Test Results` is gated `if: ${{ !cancelled() }}`, so a job GitHub kills for exceeding `timeout-minutes` uploads no JUnit artifact at all | `harness-shape.md` §4 (the raw no-timeout count) superseded in framing by `python-subprocess/process-control.md` §1, which read `pip`'s `scripttest` source, pytest's `Pytester.run()`, and uv's `TestContext` directly (via `scout-cli-acceptance.md`) and found zero of three ship a default per-call timeout — then separately read both real `verify-deep.yml` files and both `taskfile.yml`s to prove the CI-side gap is real and fleet-specific, not a corpus-wide norm. Re-confirmed: `grep -n timeout-minutes ocx/.github/workflows/verify-deep.yml grimoire/.github/workflows/verify-deep.yml` → no match in either | latent | Adopt `pytest-timeout` (`--timeout=<N>` in `addopts`, one line per `test/pyproject.toml`) rather than a per-call runner `timeout=` — a per-call value has to cover a cold binary start + Docker pull + registry round-trip in one number, a real flaky-test-creation risk the corpus reading flagged. Pair with a bounded `timeout-minutes` on both `acceptance-tests` jobs (covers hangs outside a test body, which a per-test timer doesn't) and restore `-v`/set `PYTHONUNBUFFERED=1` in the CI step regardless — that last change alone is what makes "what happened before the hang" visible in the log | Confirmed in 2 repos (`ocx/test`, `grimoire/test`) for both the runner shape and the CI-config gap. Runner shape near-certain (not independently verified) in `ocx-sion`/`ocx-soraka`/`ocx-evelynn` (byte-identical trees per `harness-shape.md`); CI-config gap not checked in siblings | mechanical: `pytest-timeout` dependency + `addopts` line, `timeout-minutes:` value, `-v`/`PYTHONUNBUFFERED` restoration are all one-line-per-file config edits, no signature change |
| FX-08 | `ocx/test/sigstore/generate-trusted-root.py:38` | The one genuinely bare `subprocess.run(...)` in the fleet with no `check=` and no `timeout=` — everywhere else the "no check" pattern is `runner.py`'s own manual `if check and result.returncode != 0: raise` one line below (a ruff blind spot, not a real gap); this site has neither | `pyright-triage.md` §2/`lint-yield.md` §4 spot-check (`PLW1510 subprocess-run-without-check`), corroborated by `codified-reconciled.md` §"Staged path" ("a blanket ignore would hide the genuinely-bare `subprocess.run` in `sigstore/generate-trusted-root.py:38`"). Re-confirmed: `sed -n '35,40p' ocx/test/sigstore/generate-trusted-root.py` | latent | Add `check=True, timeout=<N>` | 1 repo, 1 site | mechanical |
| FX-17 | `grimoire/test/tests/test_fix_locking.py:102` | Live, unmitigated pipe deadlock: N concurrent `Popen(..., stdout=PIPE, stderr=PIPE)` reaped in a batch via bare `p.wait()` — nothing ever drains the pipes. Real, not theoretical: Linux's default pipe capacity is 65536 bytes per stream (measured via `fcntl.F_GETPIPE_SZ` on this machine); a `grim add --global --no-install` output crossing that threshold on either stream blocks the child in `write()` forever and `.wait()` never returns | `python-subprocess/process-control.md` §3, table row 1, plus a live reproduction (a child writing 200,000 bytes to an undrained pipe never returns from `.wait(timeout=8)`) | latent | Replace the batch `p.wait()` reap with `p.communicate()` per process (or a draining reader thread if true concurrent streaming is needed — not required here, every caller wants full output at the end) | 1 repo, 1 site — "one verbose-logging change away from a real CI hang" per the source dive | mechanical |
| FX-18 | `ocx/test/src/runner.py`, `grimoire/test/src/runner.py` — both `run()` methods | Neither runner passes `encoding=`/`errors=`; `text=True` alone resolves to `locale.getpreferredencoding(False)`, not a fixed UTF-8 — fine on a normal Linux/macOS runner, but a CI image with `LANG=C`/`LC_ALL=POSIX` would raise `UnicodeDecodeError` from inside `subprocess.run()` on any non-ASCII CLI output, reproducing on one image and not another | `python-subprocess/process-control.md` §4, citing the stdlib `subprocess.Popen` docs directly for the locale-fallback behavior | latent | Add `encoding="utf-8", errors="replace"` to both — `errors="replace"` specifically, not `"strict"`: this harness asserts on CLI *behavior* in captured text, unlike a published library where a mis-decoded byte could corrupt data the caller re-emits | 2 repos, 1 line each | mechanical |
| FX-19 | `ocx/test/tests/test_project_crash_recovery.py:144`, `ocx/test/tests/test_project_concurrency.py:289-290` | Both call `.terminate()`/`.wait(timeout=...)` on the direct child only, no `os.killpg` — proven live (separate reproduction, same mechanism) that a `Popen`-spawned grandchild survives a direct-child-only kill; any grandchild these two tests' target commands spawn is orphaned today, not hypothetically | `python-subprocess/process-control.md` §2, live reproduction: a grandchild process confirmed still alive by PID check after the parent was killed via `.wait(timeout=)`+`.kill()` with no process-group isolation; `os.killpg(os.getpgid(pid), SIGKILL)` on the same scenario confirmed the grandchild gone. The fleet already has the correct pattern once, independently: `ocx/test/tests/test_lazy_loading.py:213-221` uses `start_new_session=True` + `communicate(timeout=)` + `killpg` | latent | Add `start_new_session=True` at spawn and `os.killpg(os.getpgid(proc.pid), SIGKILL)` in the timeout/kill path, copying `test_lazy_loading.py:213-221` verbatim. Needs a Windows branch if generalized further: both harnesses run under `windows-latest` in CI (confirmed: `ocx/.github/workflows/build-windows-shims.yml:262`), and `os.killpg` is POSIX-only — raises `AttributeError` there, not a silent no-op | 1 repo, 2 sites; the correct pattern already exists once in the same repo at a third site | mechanical, copy the existing sibling pattern |
| FX-09 | `ocx/test/src/assertions.py:35` | `assert not path.exists() and not _is_link(path)` — a composite assert; on failure the message can't tell which half failed (`PT018`) | `codified-reconciled.md` §PT row, cited directly with this exact line. Re-confirmed: `sed -n '34,36p' ocx/test/src/assertions.py` | hygiene | Split into two asserts, or one assert with an f-string naming which check failed | This file is **byte-identical** between `ocx/test` and `grimoire/test` (confirmed in `harness-shape.md`'s helper-layer diff — `diff` exit 0) — fixing it once and syncing both copies clears both | mechanical |
| FX-10 | `ocx/.claude/hooks/hook_utils.py:244,270`; `ocx/test/src/helpers.py:175` | 3 text-mode `open(..., "a")`/`open(..., "w")` calls with no `encoding=` kwarg — locale-dependent behavior, silently different off a UTF-8-default system | `shipped-python.md` §5 found the first two (`hook_utils.py:244,270`, stated as "the only two ... anywhere audited" — true for *that audit's scope*, which did not include `ocx/test/src/`); `exemplar-patterns.md` pattern 6's "does it survive contact with shape 1" check independently found the third, `ocx/test/src/helpers.py:175`, flagging it as a genuinely new miss outside the first audit's scope, not a contradiction of it. Re-confirmed all three: `sed -n '244p;270p' ocx/.claude/hooks/hook_utils.py` and `sed -n '175p' ocx/test/src/helpers.py` | hygiene | Add `encoding="utf-8"` to all three | `hook_utils.py` is a **fork**, not identical, between `ocx` and `grimoire` (`config-inventory.md` §6 — confirmed diverged via `diff -q`) — grimoire's copy needs checking separately, not assumed clean | mechanical |
| FX-11 | `ocx-mirror-sdk/.claude/rules/quality-errors.md`, `quality-enums.md` | Both files ship with **no frontmatter at all** (`head -1` returns `# Error Handling` / `# Enums`, not `---`), so both load always-on every session in that repo — 192 lines of project-specific Python error-handling/enum guidance paid regardless of what file is open | `config-inventory.md` §4, row #12-13. Re-confirmed directly: `head -3 ocx-mirror-sdk/.claude/rules/quality-errors.md quality-enums.md` — both start with a `#` heading, no `---` block | hygiene | Add `paths: src/ocx_mirror_sdk/**, tests/**` (or the repo's own convention) frontmatter to both, matching every other project-specific rule file in the fleet | 1 repo, 2 files, 192 lines/session | mechanical |
| FX-12 | `ocx/.claude/rules/quality-python.md:22` (byte-identical in `grimoire`, `ocx-mirror-sdk`; diff-identical in `ocx-sdk-python`) | "Bare `except:` or `except Exception:` — swallow `KeyboardInterrupt`, `SystemExit`, hide bugs" is **factually false as written**: `KeyboardInterrupt`/`SystemExit` inherit directly from `BaseException`, not `Exception` — only a truly bare `except:` catches them; `except Exception:` does not | `existing-rules-ledger.md` row #1 verified the cited `E722` fires on bare `except:` but did not check the semantic claim about what `except Exception:` catches. Verified directly this session: `python3 -c "print(issubclass(KeyboardInterrupt, Exception))"` → `False`; a live `try: raise KeyboardInterrupt() / except Exception: ... / except KeyboardInterrupt: print('not caught')` confirms `except Exception:` does not catch it | hygiene | Split the bullet: bare `except:` catches `KeyboardInterrupt`/`SystemExit`/`GeneratorExit` (all `BaseException`-only subclasses) — always name an exception; `except Exception:` hides real bugs for a *different* reason (silently continuing past an error), not that one | Shipped in **4 repos** (`ocx`, `grimoire`, `ocx-mirror-sdk` byte-identical; `ocx-sdk-python` diff-identical) — confirmed via `existing-rules-ledger.md`'s md5 check, `46b9f0ac8545b5551fa60f48d2ef2753` | mechanical text edit; needs a decision on which repo's copy is canonical / who propagates it (4 independent files, no shared source) |
| FX-13 | `ocx-sdk-python/.claude/rules/quality-python.md:23,109`, `quality-tests.md:79-80,228` (diff-identical in `ocx-mirror-sdk`) | 5 distinct claims (6 row-instances — `PT011` is cited in two separate sections of the same file) name a real ruff rule as their enforcement mechanism, but that rule's *group* is absent from either SDK repo's actual `[tool.ruff.lint] select = [...]`: `S101` (assert-for-validation), `S307` (eval/exec), `A001` (shadow-builtins), `PGH003` (bare type:ignore), `PT011` ×2 (bare `pytest.raises`) — the text reads as enforced and is not | `existing-rules-ledger.md` rows #2, #11, #12, #14, #38, #66, cross-checked against `ocx-sdk-python/pyproject.toml` and `ocx-mirror-sdk/pyproject.toml`'s real `select=[...]` lists (neither contains `S`, `A`, or `PGH`; `PT` is absent from both — confirmed directly against `tooling-posture.md`'s divergence table, §2: `ocx-sdk-python` selects `E,F,W,I,B,UP,ANN,RUF,D`, `ocx-mirror-sdk` selects `E,W,F,I,B,UP,ANN,RUF` — neither list contains `S`/`A`/`PGH`/`PT`) | hygiene | Either (a) correct the text to not claim current enforcement, or (b) add `S`, `A`, `PGH`, `PT` to both repos' `select=[...]` — `codified-reconciled.md` §2 proved this is safe to turn on: its `shape2-sdk.toml` (which includes `S`) run against the real `ocx-sdk-python` repo produced 336 violations, "adoptable now — no code-level blocker found" | 2 repos (the shared `quality-python.md`/`quality-tests.md` text), config change affects `ocx-sdk-python` + `ocx-mirror-sdk` | text fix is mechanical; enabling the groups is a policy decision (may surface real violations for the first time) |
| FX-14 | `ocx/.claude/rules/product-tech-strategy.md:33-34`, `grimoire/.claude/rules/product-tech-strategy.md` (same table) — **always-on, no `paths:` frontmatter** | Table headed "Python (Acceptance Tests)" claims `Runtime | Python 3.13+` and `Tooling | uv (Manager), Ruff (Linter)` for `test/` — but `test/pyproject.toml` declares `>=3.10` (not 3.13) and has **no `ruff` dependency anywhere** in either repo's `dev` group | `existing-rules-ledger.md` rows #92-93, cross-checked against `harness-shape.md`'s independent finding of zero lint config over `test/`. Re-confirmed: `grep requires-python ocx/test/pyproject.toml` → `>=3.10`; `grep -A6 "\[dependency-groups\]" ocx/test/pyproject.toml grimoire/test/pyproject.toml` → `pytest`, `pytest-xdist`, `pexpect`, `oras`/`rich` — no `ruff` in either | hygiene | Correct the table to match the real declared floor and dependency set (or raise the floor per FX-01/02 and add `ruff`, if that's the intended direction) | 2 repos, always-on (paid every session in both, per `meta-ai-config.md`'s own always-on-global contract) | text fix mechanical; deciding whether to fix the *text* or fix the *repo* to match the text is a repo-owner decision, out of scope for a `grimoire-lore`-authored rule set to silently resolve |
| FX-15 | `ocx/.claude/taskfile.yml:35-45` (`task .claude:tests`), `grimoire/.claude/taskfile.yml` (same) | The AI-config structural test suite (`test_ai_config.py` + `test_hooks.py`) has a working task command but **no CI workflow anywhere invokes it** — the suite exists, presumably passes, and gates nothing | `tooling-posture.md` §5. Re-confirmed directly: `grep -rln "claude:tests\|test_ai_config\|test_hooks" ocx/.github/workflows/` → no hits (the unrelated `claude.yml` Claude-mention bot workflow was checked and excluded) | latent | Add a `task .claude:tests` step to `verify-basic.yml`'s acceptance job in both repos | 2 repos have the suite unwired; `ocx-save/.claude/hooks/` has **no `.claude/tests/` directory at all** (a related but distinct gap — not unwired, genuinely absent) | mechanical CI-yaml edit for the 2 repos with the suite; `ocx-save`'s gap needs someone to decide whether to port the suite there at all |
| FX-16 | Fleet-wide (`ocx`, `grimoire`, `ocx-sdk-python`, `ocx-mirror-sdk`, `index`, `grimoire-lore`) | Zero dedicated Python governance exists in `grimoire-lore` itself — the repo that will house the new rule set — despite it carrying 815 lines of its own Python (`scripts/make-mark.py` 227 + `research-lang/scripts/check-artifacts.py` 588); both files are 100% type-annotated and pass their own `--self-test`/`--selftest` modes but are ungoverned by any rule anywhere | `config-inventory.md` §4, "gaps found." Re-confirmed: `find grimoire-lore/.claude/rules -iname 'quality-python*'` and `find grimoire-lore -iname 'quality-python*'` both return nothing | hygiene | Not a code fix — the new Python rule set itself is the fix; listed here only because it's a concrete, already-verified absence, not a recommendation | 1 repo | n/a — this is the deliverable this whole program exists to produce, not a fleet-fix-list action item |

## The four one-line wins

Best ratio of defects-closed to bytes-changed, each with a proven before/after count.

1. **`ruff check --select F821` in CI for `ocx/test` and `grimoire/test`.** Zero config needed — confirmed live on the real files (`annotations-evaluation.md` §4: `ruff check --select F821 --no-cache conftest.py` → `Found 1 error`, and `ruff check` with no `--select` on the same file → `Found 9 errors` total). Before: 0 CI coverage in either repo (`grep -rl ruff .github/workflows/` → no hits in either, confirmed). After fixing FX-03's 11 sites: 0 `F821` fleet-wide. Closes: all 11 forward-ref bugs, permanently, for a config line that costs nothing (ruff is already `on PATH` in this toolchain and needs no repo dependency to run via `uvx`).

2. **`PT` group in the two SDK repos.** `codified-reconciled.md` §2, `shape2-sdk.toml` run against the real `ocx-sdk-python` repo (not `--isolated`): `ruff check --config shape2-sdk.toml src tests` → **336 violations total** (the config also adds `A,N,C4,FLY,PIE,DTZ,BLE,ERA,PERF,C90,ASYNC,TC,FBT,T20,SLF,DOC` alongside `PT`, all measured near-zero-cost); `--fix --diff` → 181 auto-fixable safely, 155 remain, "adoptable now — no code-level blocker found." `PT018` alone catches a real bug (FX-09).

3. **Pyright standard, scoped to `test/src/` only, in `ocx/test` and `grimoire/test`.** `pyright-triage.md` §5, proven by direct run: `cd ocx/test && uvx pyright --outputjson --pythonpath .venv/bin/python src` → **7 errors, 19 files**; `cd grimoire/test && ... src` → **0 errors, 5 files**. Before: 0 type-checking anywhere over either harness (`tooling-posture.md` §2, confirmed no `[tool.pyright]` section in either `pyproject.toml`). The full-tree alternative needs 110 suppressions + 76 code fixes first (`pyright-triage.md` §4) — not a day-one gate; `src/`-only is.

4. **`PLC0415` per-file-ignore for `tests/**`/`conftest.py`, in every repo that selects the `PL` family.** Confirmed real gap: `PLC0415` is 86% of the `PL[C]` hits and is the harness's own documented, deliberate lazy-import idiom (`ocx/test/pyproject.toml:9-13`'s own comment explains why: avoid dragging `oras`/registry imports into pure-logic unit-test collection) — nothing in the fleet currently ignores it (`codified-reconciled.md` §1, "PLC0415 needs a fleet ignore it currently lacks," the one net-new gap that agent's independent measurement surfaced beyond the original `lint-yield.md` audit). Exact line, matching `grimoire-lore/ruff.toml:44-47`'s existing `per-file-ignores` style:
   ```toml
   [lint.per-file-ignores]
   "tests/*" = ["S101", "PLR2004", "S603", "PLC0415"]  # PLC0415 -- deliberate: heavy imports (oras, registry
                                                        # clients, sigstore) are lazy-loaded inside test bodies
                                                        # so a pure-logic unit test never pays their import
                                                        # cost at collection time
   ```

## Fixes that need a human decision

- **Raising `ocx/test`'s and `grimoire/test`'s `requires-python` floor (FX-01/02) is a compatibility decision, not a code fix.** Someone has to decide whether 3.10/3.11 support is still a real target for these harnesses (it currently isn't — collection fails — so the "decision" is really just picking the number that matches reality) and whether the same fix rolls out to the 6-8 sibling repos in one sweep or repo-by-repo.
- **Fixing the fleet's copy of `quality-python.md`'s false `except Exception:` claim (FX-12) is a coordination decision** — 4 independent files, no shared source, no repo "owns" the canonical copy. Someone has to pick where the correction lands first and how it propagates.
- **Correcting or enforcing the six unenforced-rule citations in `quality-python.md`/`quality-tests.md` (FX-13) is a workflow decision**, not just a text edit — enabling `S`/`A`/`PGH`/`PT` changes what CI blocks on in two shipped-package repos, even though `codified-reconciled.md` already proved the resulting violation count (336, "adoptable") is not a landmine.
- **Superseding `product-tech-strategy.md`'s wrong always-on claims (FX-14) is a decision for `ocx`'s and `grimoire`'s own repo owners**, not something a `grimoire-lore`-authored Python rule set can silently patch from outside — flagging it is this program's job; editing another repo's always-on global is not.
- **The self-type-annotation fix for `fake_gitlab.py`'s mixin pattern (FX-06) is a house-style call**, not a forced fix — the code is already correct; adding `TYPE_CHECKING`-guarded self-types is one valid answer, a documented per-file pyright suppression is another equally valid one, and the choice should match whatever convention the new rule set settles on for mixins generally, not be made once here.
- **Whether `ocx-save/.claude/hooks/` (and any future repo) gets its own `.claude/tests/` suite (FX-15's second half) is a scope decision** — it is a *fresh gap*, not the same "unwired" defect as `ocx`/`grimoire`.

## Explicitly not a defect

Things this program looked at, suspected, and cleared — checked here so nobody re-audits
them in six months.

- **Lockfiles are healthy.** All 7 relevant `uv.lock` files pass `uv lock --check` with exit 0, all git-tracked, no drift (`tooling-posture.md` §4).
- **`ocx.toml`'s `:latest` tag is digest-resolved, not a reproducibility gap.** `ocx.lock` resolves the floating tag to a concrete digest at `add`/`update` time; CI consumes the lock, not the tag (`tooling-posture.md` §4).
- **Free-threading (3.14t) is clean across the SDK and the bot.** `uv python install 3.14t`, `sys._is_gil_enabled()` confirms `False`; both `ocx-sdk-python` and `index/bot` import cleanly, collect the same test count, pass their full cheap tier, unchanged, under free-threading (`version-floor.md` §5, `shipped-python.md` §3).
- **`py.typed` is present where it should be.** `ocx-sdk-python/src/ocx_sdk/py.typed` and `ocx-mirror-sdk/src/ocx_mirror_sdk/py.typed` both exist; absent from `index/bot`, correctly, since it's an internal CLI never imported as a library (`shipped-python.md` §3).
- **`ocx-mirror-sdk`'s code is as good as its siblings' despite a looser gate.** Per-KLOC comparison against `ocx-sdk-python`/`index/bot`: 100% annotated (matches both), 0 `cast(` (better than both), 0 bare `except:`, 0 `shell=True`, real measured coverage 95% (comfortably above its own 80% floor). The `fail_under=80`/no-pyright-`strict` gap is real but shows up as *less proof*, not worse code (`exemplar-patterns.md`, "Divergences worth a decision"). This is a config-artifact gap for a checklist to catch, not a code smell for a rule to catch.
- **Coverage at 100% is real, not gamed.** Run live, not read from config: `uv run --extra dev coverage run -m pytest && coverage report` → `TOTAL 2170 0 396 0 100%` (SDK), and separately for `index/bot`, `100.00%` with `762 passed`. Both reach it via an injected I/O seam (`subprocess.Popen` factory / `Protocol`-typed adapter ports), not by skipping the hard paths (`shipped-python.md` §3, §7; `exemplar-patterns.md` pattern 5).
- **The fleet's lack of per-call subprocess timeouts is not evidence of neglect; pip, pytest, and uv all do the same.** `python-subprocess/process-control.md` §1, corroborating `scout-cli-acceptance.md`'s direct source reads: `pip`'s `scripttest.TestFileEnvironment.run()` has no `timeout=` anywhere in its call chain; pytest's own reference `Pytester.run()` defaults `timeout: float | None = None`, opt-in and off; uv's `TestContext` has no harness-level timeout, only `with_http_timeout()` for the binary's own internal HTTP client. Zero of three ship a default per-call timeout. **What *is* a defect is the missing kill path and the undrained pipe, which are separable from the timeout question** — see FX-17 (live pipe deadlock), FX-19 (process-group orphaning proven live), and FX-07 (the CI-side attribution gap: no `timeout-minutes`, `-v` silently dropped, JUnit lost on a cancelled job — a fact about this fleet's own CI config, not an opinion about timeout design in the abstract).

## Dropped

One item from the commissioning message's candidate list was wrongly dropped in the
first pass and is corrected below; one other did not survive verification as written:

1. **"The absence of subprocess timeouts matches what pip, pytest and uv all do."**
   **Correction: this item did not survive re-verification against the full artifact
   set and is un-dropped.** The original check only covered `python-audit/` and
   `python-topic-map/`'s ten headers; `scout-cli-acceptance.md` — classified from its
   header as an external-corpus scout with no fleet defects — in fact reads `pip`,
   `pytest`, and `uv`'s own test-infrastructure *source* directly and supports this
   claim exactly (§"Where our harness is unusual" above). The claim is real and now sits
   in "Explicitly not a defect," correctly narrowed: the *absence of a timeout* is
   industry-standard; the *missing kill path* (FX-19) and *undrained pipe* (FX-17) are
   not, and neither is this fleet's own CI-attribution gap (folded into FX-07). Nothing
   was dropped in the end — this row records that the header-level read that excluded
   `scout-cli-acceptance.md` was the error, not the claim.
2. **The exact wording "six claims in the adopted rule files whose named lint exists but
   is absent from the repo's actual `select=[...]`".** Verified the underlying finding
   (now **FX-13**) but the precise count is **5 distinct rule mechanisms across 6 row
   citations** — `PT011` is named twice, in two different sections of the same file
   (`quality-tests.md:79-80` and `:228`), for the same underlying rule. Reported as 5
   mechanisms / 6 citations rather than a flat 6, per `existing-rules-ledger.md`'s own
   row-level accounting (rows #2, #11, #12, #14, #38, #66).
